<Window
    x:Class="CopyMerge.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CopyMerge"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="CopyMerge"
    Width="900"
    Height="600"
    MinWidth="800"
    MinHeight="550"
    Closing="OnClose"
    Icon="/icon.ico"
    IsVisibleChanged="OnIsVisibleChanged"
    StateChanged="OnStateChanged"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            
            
            <FontFamily x:Key="JetBrainsMono">pack://application:,,,/Fonts/#JetBrains Mono</FontFamily>

            <SolidColorBrush x:Key="PrimaryBackground" Color="#1e1e1e"/>
            <SolidColorBrush x:Key="SecondaryBackground" Color="#2d2d30"/>
            <SolidColorBrush x:Key="CardBackground" Color="#252526"/>
            <SolidColorBrush x:Key="AccentColor" Color="#0d7377"/>
            <SolidColorBrush x:Key="AccentHover" Color="#14a085"/>
            <SolidColorBrush x:Key="AccentPressed" Color="#0a5d61"/>
            <SolidColorBrush x:Key="BorderColor" Color="#3c3c3c"/>
            <SolidColorBrush x:Key="TextPrimary" Color="#ffffff"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#cccccc"/>
            <SolidColorBrush x:Key="TextMuted" Color="#999999"/>
            
            
            <Style x:Key="ModernCard" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource CardBackground}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="Margin" Value="0,0,0,16"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.4"/>
                    </Setter.Value>
                </Setter>
            </Style>
            
            
            <Style x:Key="ModernButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="20,12"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Height" Value="44"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="6"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHover}"/>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#0d7377" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource AccentPressed}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#404040"/>
                                    <Setter Property="Foreground" Value="#666666"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            
            <Style x:Key="ThickActionButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
                <Setter Property="Height" Value="56"/>
                <Setter Property="Padding" Value="24,16"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
            
            
            <Style x:Key="ModernComboBox" TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Height" Value="44"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                            Grid.Column="2"
                                            Focusable="False"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="MainBorder"
                                                    Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    CornerRadius="6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="30"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <Path Grid.Column="1"
                                                          x:Name="DropDownArrow"
                                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                                          Fill="{StaticResource TextSecondary}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                                    <Setter TargetName="DropDownArrow" Property="Fill" Value="{StaticResource AccentColor}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                
                                <ContentPresenter x:Name="ContentSite"
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="{TemplateBinding Padding}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                
                                <TextBox x:Name="PART_EditableTextBox"
                                        Style="{x:Null}"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"
                                        Focusable="True"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{TemplateBinding Foreground}"
                                        Visibility="Hidden"
                                        IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                
                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Grid x:Name="DropDown"
                                          SnapsToDevicePixels="True"
                                          MinWidth="{TemplateBinding ActualWidth}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder"
                                                Background="{StaticResource CardBackground}"
                                                BorderBrush="{StaticResource BorderColor}"
                                                BorderThickness="1"
                                                CornerRadius="6"
                                                Margin="0,2,0,0">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="4" Opacity="0.5"/>
                                            </Border.Effect>
                                            <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True"
                                                          VerticalScrollBarVisibility="Hidden"
                                                          HorizontalScrollBarVisibility="Hidden">
                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                            
                            <ControlTemplate.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="#666666"/>
                                </Trigger>
                                <Trigger Property="IsGrouping" Value="True">
                                    <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                                </Trigger>
                                <Trigger SourceName="PART_Popup" Property="Popup.AllowsTransparency" Value="True">
                                    <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="6"/>
                                    <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                                </Trigger>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter Property="IsTabStop" Value="False"/>
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            
            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemBorder"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource AccentColor}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource AccentHover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="WindowControlButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Width" Value="46"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#60FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            
            <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#e81123"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource PrimaryBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

                
            <Border Grid.Row="0" 
                    Background="{StaticResource SecondaryBackground}" 
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    
                    <StackPanel Grid.Column="0" 
                               Orientation="Horizontal" 
                               VerticalAlignment="Center"
                               Margin="12,0,0,0">
                        <TextBlock Text="🚀" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="CopyMerge" 
                                  FontFamily="{StaticResource JetBrainsMono}" 
                                  FontSize="18"
                                  FontWeight="Medium"
                                  Foreground="{StaticResource TextSecondary}" 
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    
                    <StackPanel Grid.Column="1" 
                               Orientation="Horizontal">
                        <Button x:Name="MinimizeButton"
                                Content="&#xE921;"
                                Style="{StaticResource WindowControlButton}"
                                Click="MinimizeButton_Click"/>
                        <Button x:Name="MaximizeButton"
                                Content="&#xE922;"
                                Style="{StaticResource WindowControlButton}"
                                Click="MaximizeButton_Click"/>
                        <Button x:Name="CloseButton"
                                Content="&#xE8BB;"
                                Style="{StaticResource CloseButton}"
                                Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="24,24,24,0">
            <Grid Margin="32,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0" Width="64" Height="64" VerticalAlignment="Center" Margin="10">
                    <Border Width="64" Height="64"
                            Background="{StaticResource AccentColor}"
                            CornerRadius="32">
                        <Border.Effect>
                            <DropShadowEffect Color="#0d7377" BlurRadius="12" ShadowDepth="0" Opacity="0.4"/>
                        </Border.Effect>
                    </Border>

                    <Image Source="/Logo.png"
                           Width="64" Height="64"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           Stretch="UniformToFill">
                        <Image.Clip>
                            <EllipseGeometry Center="32,32" RadiusX="32" RadiusY="32"/>
                        </Image.Clip>
                    </Image>
                </Grid>
                
                
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock FontFamily="{StaticResource JetBrainsMono}" 
                               FontSize="16"
                              TextWrapping="Wrap" LineHeight="24" Foreground="{StaticResource TextPrimary}">
                        Объединяй скопированные тексты автоматически. Используй 
                        <Run FontWeight="Bold" Foreground="{StaticResource AccentColor}">Ctrl+Shift+C</Run> 
                        для добавления к уже скопированному тексту.
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden" Margin="24,8,24,0">
            <StackPanel>
                
                
                <Border Style="{StaticResource ModernCard}">
                    <Grid Margin="32,28">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="24"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="⚙️ Разделитель текста" 
                                      FontFamily="{StaticResource JetBrainsMono}"
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                            <TextBlock Text="Выберите символ для объединения текстов"
                                      FontFamily="{StaticResource JetBrainsMono}" 
                                      Foreground="{StaticResource TextMuted}" Margin="0,0,0,16"
                                      TextWrapping="Wrap"/>
                            <ComboBox x:Name="comboBox"
                                     FontFamily="{StaticResource JetBrainsMono}"
                                     SelectedIndex="0"
                                     SelectionChanged="ComboBox_SelectionChanged"
                                     Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Enter" />
                                <ComboBoxItem Content="Space" />
                                <ComboBoxItem Content="," />
                                <ComboBoxItem Content="." />
                            </ComboBox>
                        </StackPanel>

                        
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="🚀 Автозапуск" 
                                      FontFamily="{StaticResource JetBrainsMono}"
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                            <TextBlock Text="Запускать приложение при старте системы"
                                      FontFamily="{StaticResource JetBrainsMono}" 
                                      Foreground="{StaticResource TextMuted}" Margin="0,0,0,16"
                                      TextWrapping="Wrap"/>
                            <ComboBox x:Name="comboBoxAutoran"
                                     FontFamily="{StaticResource JetBrainsMono}"
                                     SelectedIndex="1"
                                     SelectionChanged="ComboBoxAutoran_SelectionChanged"
                                     Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="✅ Включено" />
                                <ComboBoxItem Content="❌ Выключено" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </Border>

                
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel Margin="32,28">
                        <TextBlock Text="📋 История буфера обмена" 
                                  FontFamily="{StaticResource JetBrainsMono}"
                                  Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                        <TextBlock Text="Последние скопированные элементы"
                                  FontFamily="{StaticResource JetBrainsMono}" 
                                  Foreground="{StaticResource TextMuted}" Margin="0,0,0,20"
                                  TextWrapping="Wrap"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <ComboBox x:Name="ClipboardHistoryCombo"
                                     Grid.Column="0"
                                     FontFamily="{StaticResource JetBrainsMono}"
                                     ItemsSource="{Binding ClipboardStore, Mode=OneWay}" 
                                     SelectedItem="{Binding FirstNote, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernComboBox}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" MaxWidth="400" TextTrimming="CharacterEllipsis"
                                                  FontFamily="{StaticResource JetBrainsMono}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            
                            <Button Grid.Column="2" Content="Копировать" 
                                    Style="{StaticResource ModernButton}"
                                    Click="CopySelectedNote_Click"/> 
                            
                            <Button Grid.Column="4" Content="Очистить"
                                   Style="{StaticResource ModernButton}"
                                   Click="ClearClipboardHistory_Click"/> 
                        </Grid>
                        
                        
                        <Border Background="{StaticResource SecondaryBackground}" 
                                CornerRadius="6" 
                                BorderThickness="1"
                                BorderBrush="{StaticResource BorderColor}"
                                Margin="0,16,0,0">

                            <StackPanel>
                                <TextBlock Text="Перевью буфера"
                                           FontFamily="{StaticResource JetBrainsMono}" 
                                           Foreground="{StaticResource TextPrimary}" 
                                           FontWeight="Bold"
                                           Margin="16,12,16,0"/>

                                <TextBlock Text="{Binding ClipboardPreview}" 
                                           FontFamily="{StaticResource JetBrainsMono}" 
                                           xml:space="preserve"
                                           Foreground="{StaticResource TextMuted}" 
                                           Margin="16,8,16,12"
                                           TextWrapping="Wrap" 
                                           MaxHeight="120"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </Border>

                
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel Margin="32,28">
                        <TextBlock Text="⚡ Быстрые действия" 
                                  FontFamily="{StaticResource JetBrainsMono}"
                                  Foreground="{StaticResource TextPrimary}" Margin="0,0,0,20"/>
                        
                        <UniformGrid Rows="1" Columns="3">
                            <Button Content="📖 Руководство" Style="{StaticResource ThickActionButton}" 
                                   Margin="0,0,8,0" Click="ShowHelp_Click"/>
                            <Button Content="🔗 Поддержка" Style="{StaticResource ThickActionButton}" 
                                   Margin="8,0,8,0" Click="OpenTelegram"/>
                            <Button Content="ℹ️ О программе" Style="{StaticResource ThickActionButton}" 
                                   Margin="8,0,0,0" Click="ShowAbout_Click"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        
        <Border Grid.Row="3" Background="{StaticResource SecondaryBackground}" 
                BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderColor}">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" VerticalAlignment="Center"
                          FontFamily="{StaticResource JetBrainsMono}"
                          Foreground="{StaticResource TextMuted}">
                    <Run Text="© 2025 Wixon"/>
                    <Run Text=" • "/>
                    <Hyperlink Click="OpenTelegram" Foreground="{StaticResource AccentColor}" TextDecorations="None">
                        <Run Text="Кодерские работы"/>
                    </Hyperlink>
                    <Run Text=" • С уважением к каждому клику ❤️"/>
                </TextBlock>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="v2.0" FontFamily="{StaticResource JetBrainsMono}" 
                              Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>